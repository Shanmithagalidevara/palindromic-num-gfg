# Your task is to complete this function
# Function should return true/false
def PalinArray(arr):
    # Code here
    for s in arr:
        p=s
        s=str(s)
        if p!=int(s[::-1]):
            return False
    return True

